#include "main.h"
/**
   * mul - Makes the multiplcation of of two numbers
    * @a: First operand
     * @b: Second operand
      *
       * Return: The multiplcation of the two parameters
	*/
int mul(int a, int b)
{
	return (a * b);

}

